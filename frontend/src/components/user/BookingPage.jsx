import React from 'react'

const BookingPage = () => {

  const { id } = useParams();
  const [officeData, setOfficeData] = useState(null);

  return (
    <div>

    </div>
  )
}

export default BookingPage;